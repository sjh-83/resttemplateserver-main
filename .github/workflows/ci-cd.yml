name: CI/CD Pipeline

on:  
  push:
    branches:
      - main

env:
  HARBOR_REGISTRY: 210.90.24.172:8080
  HARBOR_PROJECT: restproject
  STUDENT_ID: shim
  IMAGE_NAME: resttemplateserver

jobs:
  build-and-push:
    name: Build & Push to Harbor
    runs-on: self-hosted

    outputs:
      image_tag: ${{ steps.meta.outputs.tag }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Maven Wrapper
        shell: cmd
        run: mvnw.cmd clean package -DskipTests

      - name: Set image tag
        id: meta
        shell: cmd
        run: echo tag=%GITHUB_SHA%>> %GITHUB_OUTPUT%
        
      - name: Log in to Harbor
        uses: docker/login-action@v3
        with:
          registry: ${{ env.HARBOR_REGISTRY }}
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5 
        with:
          context: .
          push: true
          tags: |
            ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/${{ env.STUDENT_ID }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.tag }}
            ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/${{ env.STUDENT_ID }}/${{ env.IMAGE_NAME }}:latest

  update-manifest:
    name: Update K8s Manifest for ArgoCD
    runs-on: self-hosted
    needs: build-and-push

    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update image tag in k8s.yaml
        shell: powershell
        run: |
          $newImage = "${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/${{ env.STUDENT_ID }}/${{ env.IMAGE_NAME }}:${{ needs.build-and-push.outputs.image_tag }}"
          (Get-Content k8s.yaml) -replace 'image: .*resttemplateserver.*', "image: $newImage" | Set-Content k8s.yaml

      - name: Commit and push manifest update
        shell: powershell
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s.yaml
          $diff = git diff --staged --quiet; if ($LASTEXITCODE -ne 0) { git commit -m "ci: update image to ${{ needs.build-and-push.outputs.image_tag }}"; git push }